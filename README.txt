To run the website you must have flask installed along with a virtual envirement because we were
not able to host the website. Screen shots have been provided of each algorithm being test. The 
checkbox above some algorithems signifies wether you want to decrypt(unchecked) encrypt(checked).
Please do no remove Main.html from the templates folder as the programs relies on it being there.
Screenshots are in the SC Folder
